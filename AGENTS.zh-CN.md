---
source_language: zh-CN
translation_status: source
---

[English](AGENTS.md)

# Weftext 仓库规则

## 产品边界

- Weftext 使用 Rust 原生实现。
- 生产文档转换使用 Weftext 自有导入基础设施，不依赖其他产品的元数据、适配器、清单、线协议或发布版本。
- 公开仓库文档只描述当前 Weftext 契约。产品比较、截图、研究笔记、排期、任务交接、决策历史和验收日志保存在仓库之外。
- 面向读者的公开 Markdown 以中文为源，并维护同步英文译文。测试夹具、生成证据、锁文件和机器可读样例不做本地化。

## 存储权威

- 产品只有一种受管节点形态：`X/X.adoc`，由根目录 `.weftext-format` 中精确的 `weftext.asciidoc.v1\n` 字节选择。Markdown 只用于显式导入/导出、可见非受管内容或节点附件，不是同级受管格式。
- 根目录 `.weftext-rules` 可将普通路径分类为可见非受管内容或忽略内容。Core 是唯一清单权威；各外壳不得自行重新发现节点。
- 非受管目录是完整的子树边界，没有 UUID 或 `weftext` 信封，扫描不会重新进入其中。受管节点的同名 AsciiDoc 文档不能单独分类。
- 普通非规范文件均为资源。只有当所在边界是受管节点时，才派生所有者 UUID。
- 节点身份是 `weftext.id` 中的小写 UUIDv4。路径只是定位符。父子关系来自目录树；不得持久化父节点、路径、名称或子路径列表。
- 默认子节点顺序为自然名称顺序。排序策略由父节点持有；子节点只保存稀疏手动排序值。
- 可移植节点批注使用 `weftext.annotations.json`；不得在节点内新增元数据目录。
- 原生 AsciiDoc 待办项无身份且受修订约束。持久任务是带封闭 `weftext-task` 文档头属性配置的普通受管节点，使用节点现有 UUID。不得引入另一套任务身份命名空间或任务数据库。
- 搜索、反向链接、关系图、缩略图、集合、看板和统计均为可重建派生结果。同步 SQLite 数据库不能作为内容权威。
- `.weftext-rules` 是可移植工作区权威，不从 `.gitignore` 继承。忽略字节仍属于备份内容，但不进入产品清单和投影。
- 保留名称按角色划分：根控制项使用 `.weftext-*`；封闭存储内部项可使用 `_weftext.*`；可移植节点本地伴随文件使用 `weftext.*`。

## 编辑与安全

- 结构变更使用 Core 计划和事务，不使用 UI 特有的文件操作。
- Core 拒绝忽略或非受管边界内的目标，也拒绝对含有此类边界的受管子树执行结构变更。
- 同步部分到达时，元数据缺失表示状态不完整，绝不表示可以重新生成身份。
- 保留精确 AsciiDoc/YAML 格式。系统信封补丁必须范围狭窄，并在 YAML 有歧义时安全拒绝。
- 修改持久格式或领域规则前阅读当前规范；在同一变更中更新规范和测试。

## 产品界面

- Desktop、WebUI、Server、CLI 和经批准的智能体调用方使用同一组 Core 操作与事务。
- WebUI 支持普通内网使用，不只用于管理。
- Server 客户端绝不直接写入托管工作区；认证、ACL、修订、信息不披露和审计在 Server 边界执行。
- 同步文件夹不是协作服务器。实时多用户编辑需要 Weftext Server。
- 状态报告须准确说明已经实现的产品端和剩余缺口；只有 Core 原语不等于产品功能已经完成。

## 工程

- Core 行为可无界面测试，并由当前及未来外壳共享。
- 禁止不安全 Rust。
- 仅当标准库和现有依赖不足时新增依赖，并记录许可证与可移植性影响。
- 对活动代码运行 `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings` 和 `cargo test --workspace`。
