---
source_language: zh-CN
translation_status: source
---

[English](ROADMAP.md)

# Weftext 公开路线图

Weftext 尚处于发布前阶段。本文只说明当前公开基础、已经接受的产品方向和发布缺口。内部排期、研究材料、决策历史和验收日志保存在公开源代码仓库之外。

## 当前基础

- Rust Core 统一负责受管节点身份、内容分类、精确修订、保留源文本的编辑、可恢复工作区事务、搜索/索引投影、链接、批注、Chrono、Query 计划和共享导航。
- 受管工作区只有一种格式：`.weftext-format` 选择 `weftext.asciidoc.v1`；节点使用 `X/X.adoc`；系统 YAML 只有一个顶层 `weftext` 映射；可移植审阅状态使用 `weftext.annotations.json`；`.weftext-rules` 对可见非受管内容和忽略内容进行分类。
- 基于条目的工作区回收站使用 `.weftext-trash/_weftext.items/<trashItemId>`、封闭清单、禁止覆盖事务、基于 UUID 的恢复、显式永久删除、恢复日志、同步诊断和物理备份覆盖。
- 重命名、移动、复制、移入回收站、恢复和 Chrono 变更均通过共享 Core 边界使用冻结的类型化目标、精确作用域、修订/草稿门控、可恢复日志和回执。
- 规范 Query 只有一种 `[.weftext-query,version=1,view=...]` 文字块语法，使用显式 `from` 别名、类型化作用域、词法 `this`、显式时间/区域上下文、稳定输出身份、权限过滤和资源上限。当前实现的表达式子集小于完整的 `weftext.expr.v1` 语言。
- Windows 桌面应用、CLI、仅限回环地址的服务器基础、共享 React UI 和同源浏览器 UI 使用同一 Core 权威。服务器尚不可用于部署。

## 已接受的产品方向

- Weftext AsciiDoc Profile 是唯一规范受管源语言。Markdown 只存在于显式导入/导出、可见非受管内容或节点附件边界。
- 提供保留源文本的受管节点编辑器，包括功能区选项卡、上下文“表格”和“图片”选项卡、检查器、格式刷、标题/表格结构命令、可访问的键盘等价操作和精确源文本回退。
- 为显式打开的单个外部 `.adoc` 或 `.asciidoc` 文件提供独立 AsciiDoc 模式。该模式不扫描或采用所在目录，并禁用所有仅适用于工作区的能力。
- 原生待办项保持无身份。需要持久身份或类型化字段时，通过一次可恢复事务提升为普通任务节点；使用节点 UUID 作为任务身份，并在源位置留下稳定节点链接。
- 模板库是一个配置的特殊受管子树。模板不进入普通搜索、Query、任务、关系图和时间线投影，并通过一次可预览的 Core 事务实例化完整、经过验证的子树。
- Query 谓词和模板绑定统一使用纯函数、确定性且有边界的 `weftext.expr.v1`，但 Query 子句与模板占位符语法保持不同。
- 支持 Query 驱动的节点/任务/标题集合，并提供可视化筛选、排序、分组、聚合、表格、日历、时间线和看板配置。UI 修改规范 Query 源文本，不创建隐藏的平行模型。
- 每个多维表格保存为一个受管表格节点，每行记录对应一个有边界的 JSON 文件。记录不是节点，也不能提升为节点；关系和富文本/图片值使用类型化 JSON 形式及表级事务。
- 为类型化数据源提供统一看板外壳。任务看板只是一个预设，不是独立权威；拖动、新建和编辑都映射为数据源特定的已验证操作。
- 区分设备本地近期历史与持久备份仓库。允许用户配置本地或远程备份目标与执行器；通过 Core 计划提供无需 Git 的文档比较、合并和恢复。
- 建立统一的真实格式导入流水线。PDF 是首个计划中的转换切片；所有导入都经过探测、有边界工作进程、规范化 IR、预览、事务和回执边界。
- 在不授予智能体、浏览器或 UI 外壳直接文件系统写入权威的前提下，继续发展受监督智能体操作和认证协作。

## 发布缺口

- 在所有签名打包 GUI 产品端完成生产级 AsciiDoc 编辑器及全部功能区/上下文命令；
- 完成任务节点提升、完整 `weftext.expr.v1` 求值器、Query 功能入口、模板库引擎/设计器/实例化、多维表格编辑、看板、历史、比较和备份仓库；
- 完成生产级 PDF 及其他文档导入/导出路径；
- 完成服务器的账户、角色、子树 ACL、信息不披露、持久审计、部署、备份演练和实时协作；
- 完成可访问性、输入法、Windows 10/11、崩溃恢复、长文档、大工作区、签名、更新、SBOM 和全新安装验收；
- 在测试与发布规范中的适用门槛通过之前不发布公开版本。

稳定契约见 [`docs/specifications`](docs/specifications)，当前架构决策见 [`docs/architecture`](docs/architecture)。
