{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop window permissions for selecting a Weftext workspace.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
