{
  "name": "weftext-server-webui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "lint": "node --check api.js && node --check app.js && node --check build.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs"
  }
}
